rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files are served with short-lived signed URLs issued by the authenticated API.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
