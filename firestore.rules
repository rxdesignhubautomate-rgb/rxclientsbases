rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The CRM is server-owned. Firebase Admin bypasses rules; clients use the authenticated API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
